name: TC19 - Non-numeric profile_id

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: sedstart/runcloud@v1
        with:
          api_key: ${{ secrets.API_KEY_A }}
          project_id: 418
          test_id: 1974
          profile_id: chrome
          browser: chrome
          headless: false
          environment: "QA"
