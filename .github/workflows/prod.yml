name: TC23 - All parameters invalid

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: sedstart/runcloud@v1
        with:
          api_key: invalid-key
          project_id: 999
          test_id: 999
          profile_id: 999
          browser: chrome
          headless: false
          environment: "QA"
