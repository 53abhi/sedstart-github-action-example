name: TC04 - API B with Project 418

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: sedstart/runcloud@v1
        with:
          api_key: ${{ secrets.API_KEY_B }}
          project_id: 418
          test_id: 2170
          profile_id: 900
          browser: chrome
          headless: false
          environment: "QA"
