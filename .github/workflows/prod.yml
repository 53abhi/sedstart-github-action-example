name: TC07 - Project 418 wrong profile

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: sedstart/runcloud@v1
        with:
          api_key: ${{ secrets.API_KEY_A }}
          project_id: 418
          test_id: 1974
          profile_id: 900
          browser: chrome
          headless: false
          environment: "QA"
