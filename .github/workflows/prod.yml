name: TC18 - Non-numeric project_id

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: sedstart/runcloud@v1
        with:
          api_key: ${{ secrets.API_KEY_A }}
          project_id: 398
          test_id: abc
          profile_id: 848
          browser: chrome
          headless: false
