name: TC23 - Local All parameters invalid

on:
  workflow_dispatch:

jobs:
  sedstart-run:
    runs-on: windows-latest
    steps:
      - name: Install Playwright
        run: npm install -g playwright@1.52.0

      - name: Trigger SedStart test run
        uses: sedstart/runlocal@v1
        with:
          api_key: invalid-key
          project_id: 999
          test_id: 999
          profile_id: 999
          browser: chrome
          headless: false
          environment: QA
