name: TC11 - Random test_id

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: sedstart/runcloud@v1
        with:
          api_key: ${{ secrets.API_KEY_A }}
          project_id: 418
          test_id: 9999
          profile_id: 848
          browser: chrome
          headless: false
          environment: "QA"
