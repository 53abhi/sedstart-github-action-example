name: TC02 - API Key A with Project 398

on:
  workflow_dispatch:

jobs:
  tc02:
    runs-on: ubuntu-latest
    steps:
      - name: Run TC02
        uses: sedstart/runcloud@v1
        with:
          api_key: ${{ secrets.API_KEY_A }}
          project_id: 612
          test_id: 4905
          profile_id: 1306
          browser: chrome
          headless: false
