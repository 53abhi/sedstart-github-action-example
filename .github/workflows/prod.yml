name: TC08 - Local Project 398 wrong profile

on:
  workflow_dispatch:

jobs:
  sedstart-run:
    runs-on: windows-latest
    steps:
      - name: Install Playwright
        run: npm install -g playwright@1.52.0

      - name: Trigger SedStart test run
        uses: sedstart/runlocal@v1
        with:
          api_key: ${{ secrets.API_KEY_B }}
          project_id: 398
          test_id: 2170
          profile_id: 848
          browser: chrome
          headless: false
          environment: QA
