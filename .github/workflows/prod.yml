name: TC06 - Local Project 398 with test 1974

on:
  workflow_dispatch:

jobs:
  sedstart-run:
    runs-on: windows-latest
    steps:
      - name: Install Playwright
        run: npm install -g playwright@1.52.0

      - name: Trigger SedStart test run
        uses: sedstart/runlocal@v1
        with:
          api_key: ${{ secrets.API_KEY_B }}
          project_id: 398
          test_id: 1974
          profile_id: 900
          browser: chrome
          headless: false
          environment: QA
