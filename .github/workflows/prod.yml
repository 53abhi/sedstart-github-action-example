name: TC14 - Missing project_id

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: sedstart/runcloud@v1
        with:
          api_key: ${{ secrets.API_KEY_A }}
          test_id: 1974
          profile_id: 848
          browser: chrome
          headless: false
          environment: "QA"
