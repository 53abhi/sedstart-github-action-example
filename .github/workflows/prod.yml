name: TC09 - Invalid API Key

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: sedstart/runcloud@v1
        with:
          api_key: invalid-key-123
          project_id: 418
          test_id: 1974
          profile_id: 848
          browser: chrome
          headless: false
          environment: "QA"
