name: TC03 - API A with Project 398

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: sedstart/runcloud@v1
        with:
          api_key: ${{ secrets.API_KEY_A }}
          project_id: 398
          test_id: 1974
          profile_id: 848
          browser: chrome
          headless: false
          environment: "QA"
